const str1 = "This is a string Primitive.";
const str2 = "This is a another string Primitive.";

const str3 = new String("A way to write String Object.");

console.log(str1);
console.log(str2);
console.log(str3);

// Character acess

"cat".charAt(1);

// Comparing Strings

const a = "a";
const b = "b";

if (a < b) {
    console.log(`${a} is less than ${b}`);
} else if (a > b) {
    console.log(`${a} is greater than ${b}`);
} else {
    console.log(`${a} and ${b} are equal`);
}

